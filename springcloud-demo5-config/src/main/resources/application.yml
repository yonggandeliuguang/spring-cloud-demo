server:
  port: 58083
spring:
  application:
    name: springcloud-config
  cloud:
    config:
      server:
        git:
          uri: git@github.com:yonggandeliuguang/cloudconfig-new.git #指定github仓库地址
